"""Repo-relative paths for CIFAR-10 classification experiments (ViT from scratch).

LEVIR-CD / change-detection artifacts use ``outputs/``; CIFAR-10 figures and
evaluation outputs use ``results_cifar10/`` at the repository root (and under
``outputs/train/`` when produced by ``scripts/train_cifar10.py``).
"""

from __future__ import annotations

CIFAR10_RESULTS_DIR = "results_cifar10"
