# Notebooks

| Notebook | Contents |
|----------|----------|
| `training_and_ablation_studies.ipynb` | End-to-end CIFAR ViT training and ablation experiments (notebook form of `scripts/train_cifar10.py` / `scripts/run_ablations.py`). |
| `03_train_eurosat.ipynb` | EuroSAT classification experiment (paths may need updating for your environment). |

Prefer **headless** runs for long jobs: use the `scripts/` CLIs with `tmux` or Slurm so disconnects do not stop training.
