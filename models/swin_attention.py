"""
Deprecated placeholder: Swin implementation lives in ``models.swin`` and
``models.siamese_swin``. Import from there instead.
"""

raise ImportError(
    "models.swin_attention is deprecated. Use "
    "'from models.swin import SwinBackbone' or "
    "'from models.siamese_swin import SiameseSwinChangeDetection'."
)
